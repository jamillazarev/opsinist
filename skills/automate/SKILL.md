---
description: A recurring job becomes an automation with a real trigger, or an honest manual one.
---

Load the **opsinist** skill ([SKILL.md](../advisor/SKILL.md)) and run this flow — the procedure is in [`automations.md`](../../automations.md).

A job that repeats gets a trigger the runtime actually fires — or is recorded as manual with its cadence, never as an automation that silently never runs.

Args: $ARGUMENTS
