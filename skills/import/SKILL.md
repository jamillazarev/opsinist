---
description: Bring work in from anywhere — a tracker, a spreadsheet, an export, a paste: mode declared once, mapping shown first, items created unassigned.
---

Load the **opsinist** skill ([SKILL.md](../advisor/SKILL.md)) and run this flow — the procedure is in [`importing.md`](../../importing.md).

Bring work in from anywhere — a tracker, a spreadsheet, an export, a paste: mode declared once, mapping shown first, items created unassigned.

Args: $ARGUMENTS
