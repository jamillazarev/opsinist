---
description: Move the machinery to a newer version, with the migration named.
---

Load the **opsinist** skill ([SKILL.md](../advisor/SKILL.md)) and run this flow — the procedure is in [`upgrading.md`](../../upgrading.md).

What changes, what to run, what it touches, and what could go wrong — an upgrade ships with its migration map or it waits.

Args: $ARGUMENTS
