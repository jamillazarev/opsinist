---
description: Move a project between machines or runtimes — the repository travels, sessions do not.
---

Load the **opsinist** skill ([SKILL.md](../advisor/SKILL.md)) and run this flow — the procedure is in [`migrating.md`](../../migrating.md).

End the session properly and open the project elsewhere: the record, roles and history are already there; what the old runtime held and the repo did not is re-created, not migrated.

Args: $ARGUMENTS
