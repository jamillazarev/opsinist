# Opsinist — context pointer

This workspace has the opsinist extension installed. **Before acting as the advisor — or on
any request about building or running a team of agents, a project, tasks, roles or budgets —
read `skills/advisor/SKILL.md` and follow it.** The hard gates that must survive even when
the router is skipped live in `rules/opsinist.md`; load them with it. Opening the skill is
reading the manual, not creating anything — "just your take" constrains the output, never the
reading.
