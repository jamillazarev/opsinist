__pycache__/
*.pyc
.DS_Store
company/

# Nothing built belongs here. A site build swept into this tree once, by a `git add -A`
# during a squash, and shipped 928K of fonts and bundles inside a package that is markdown.
docs/.vitepress/
dist/
node_modules/
.impeccable/

# a session lock is a courtesy note, never history (dispatching.md)
.opsinist-checkout
# The same scar as the block above, paid a second time on 2026-08-09: `git add -A` swept a
# browser-automation dump and a dev-server config from an unrelated project into a release —
# 1172 lines, 86 KB, published in 0.2.6 before a lens caught it. None of it is the skill.
.playwright-mcp/
.claude/launch.json
.claude/settings.local.json
.claude/worktrees/
