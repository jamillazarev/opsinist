# {{the question, as a question — not a topic}}

**Decides**: {{a link to the task this decides — `T-XXXXXX`, as a link and not a bare id — or `nothing yet`}} ·
**Status**: {{open · settled · stale}} · **Depth**: {{orienting · deciding · standing — see
`ANSWER-template.md`; guidance, not a gate: nothing has measured whether stating it changes anything}}
**Answered**: {{YYYY-MM-DD}} · **Recheck when**: {{the named event from *What would change it* below, in a few words}}

> [!NOTE]
> **A topic never closes; a question does.** Name this file for the question, and the day it is
> answered the file is finished rather than perpetually behind. *"Synthetic panels"* is a subject
> and will be open forever; *"should we pay for synthetic panels"* has an answer.

## What we now believe

{{One paragraph. This is the part read INSTEAD of everything else — an agent that needs the answer
reads this and stops, and the register underneath is for whoever argues.}}

**Confidence**: {{measured · cited · recalled · judgement call}} — the rung travels with the claim
(`permissions.md` → *the evidence ladder*), and a finding built on `cited` sources does not become `measured` by being written
down.

## What would change it

{{A named event, not "further research". *"When a replacement for `park-self-reports` ships"* is a
trigger; *"periodically"* is an abandonment wearing better clothes.}}

## Under it

**Sources**: {{where the project keeps a source register, by **id** — links rot and ids grep. Where
it does not, a full citation with its date and where the copy lives: `_ops/resources/` holds the
local file every external thing keeps. **Never a bare URL**, which is the one form that rots
silently.}}

**Earlier findings this rests on**: {{`<other-question>.md` in this directory — or `none`. **Checked before the
work started, not after**: *look inward first* is a law, and a research task that re-derives what
this project already concluded spent the owner's money to arrive where it was.}}

**Local artifacts**: {{`_ops/research/raw/…` — transcripts, exports, screenshots. Raw material is
not a finding and does not belong above.}}

---

**Why this file is not the task.** The task closes; this does not. A finding stays true until its
recheck fires, is read long after its task is archived, and is the thing the *next* question starts
from — which is why the relation runs both ways: the task links here, and here links back to the
decision it was for. **A finding with `Decides: nothing yet` is reading, and reading is what gets
read last** — the order to read these in is the order of the decisions waiting on them, and no
separate priority list is kept because a second list is a list that lies.
