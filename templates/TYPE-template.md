# {{type, in this project's own words}} — one kind of work

**Why this file exists:** every task of a kind answers to the same two bars — ready to start,
ready to finish — and writing them once here beats re-deciding them per task, or worse, per
mood. **The name above is the project's word, not software's**: an episode, a commission, an
issue, a batch — `bug` only where things are actually called bugs.

**Covers:** {{one line — what work lands under this type}}

## Ready when — may it start?

The universal floor first — workable from the task and its linked docs alone, and the outcome can
be written. Then this type's own line:

- {{what must exist before work starts. A bug: a reproduction. A commission: colour and deadline
  agreed with the client. A newsletter issue: the week's source list gathered.}}

## Done when — may it finish?

The craft gates every task of this type passes, which its own acceptance criteria then make
concrete:

- {{an episode: audio mastered, notes written, transcript checked. A batch: fired, inspected,
  defects counted. Code: tests pass, reviewed by a non-author.}}

## Checked mechanically by

{{optional — the command every task of this type runs clean before review is asked for:
the test suite, a schema validation, a link check. Leave absent where the craft has no
mechanical half; never invent one for the look of it.}}

## Where the description lives

{{the cut on the depth ladder for tasks of this type — `outcome` · `spec` · `example` —
proposed at this wave from the craft's own standards, confirmed in the owner's words. Where
the cut is `example`, state the kind: **validator-checked** (a command refuses — tests, a
schema) or **gauge-checked** (an exemplar and a judge who is not the author — a reference
batch, a model issue). A bug wants `example` because its ready-when already demands a
reproduction; a chore wants the floor. A task overrides explicitly. → `writing-work.md`}}

## Pipeline

{{the stage ladder — default: build → review → accept; a design stage in front where design
precedes build}}

## Provenance

{{where these bars came from: stock default kept as-is · researched — sources and date · the
owner's own words, date. A bar that keeps bouncing the same work proposes its own change, with
the diff and the evidence — never a silent edit: the bar is a locked surface.}}
